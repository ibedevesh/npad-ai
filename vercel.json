{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @npad/server install --offline 2>/dev/null || pnpm install",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
